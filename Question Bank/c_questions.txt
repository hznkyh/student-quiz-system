1,Write a C function to reverse a string and print it to stdout. The function must have prototype: void reverseString(char* str);
2,Write a C function to find the length of a string and print it to stdout. The function must have prototype: void stringLength(char* str);